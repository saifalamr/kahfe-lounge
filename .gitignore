node_modules/
.next/
.env.local
.env*.local
*.log
.DS_Store
tsconfig.tsbuildinfo

# Backup workflow scratch space — the actual backup data only ever lives
# on the `backups` branch, never here on main.
/backups/
/backups-branch/
